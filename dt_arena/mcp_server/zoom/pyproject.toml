[project]
name = "zoom-mcp-server"
version = "0.1.0"
description = "MCP Server exposing Zoom-like meeting tools (sandbox)"
requires-python = ">=3.10"
dependencies = [
  "fastmcp>=2.13.0",
  "mcp[cli]>=1.4.1",
  "httpx>=0.27.0",
  "uvicorn[standard]>=0.31.1",
]

[tool.uv]
dev-dependencies = []


