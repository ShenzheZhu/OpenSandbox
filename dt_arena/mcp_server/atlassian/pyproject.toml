[project]
name = "atlassian-mcp-server"
version = "0.1.0"
description = "Atlassian MCP Server for Jira/Confluence sandbox operations"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=2.13.0",
    "httpx>=0.25.0",
    "pyyaml>=6.0",
]

[tool.uv]
dev-dependencies = []
