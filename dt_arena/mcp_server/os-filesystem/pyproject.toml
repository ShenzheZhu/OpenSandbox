[project]
name = "os-filesystem-mcp"
version = "0.1.0"
description = "OS-Filesystem MCP Server for agent evaluation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.4.1",
]

