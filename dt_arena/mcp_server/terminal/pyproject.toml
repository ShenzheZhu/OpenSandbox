[project]
name = "terminal-mcp"
version = "0.1.0"
description = "Terminal MCP server for executing commands in Docker environment"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.4.1",
]
