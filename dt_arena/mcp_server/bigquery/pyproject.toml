[project]
name = "bigquery-mcp-server"
version = "0.1.0"
description = "Local BigQuery emulator MCP server"
requires-python = ">=3.10"
dependencies = [
  "fastmcp>=2.13.0",
  "google-api-core>=2.19.0",
  "google-auth>=2.35.0",
  "google-cloud-bigquery>=3.27.0",
]

[tool.uv]
dev-dependencies = []
