# Seeds

This directory is intentionally empty in the first extraction.

Add clean, task-agnostic seed data here when an environment needs reusable state for training. Do not place benchmark task files, red-team instructions, or judge fixtures in this directory.

